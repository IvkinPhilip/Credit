openapi: 3.0.0
info:
  title: 'CreditData'
  version: '1.0'
servers:
  - url: 'http://localhost:3000'
paths: {}
components:
  schemas:
    Credit-Information:
      title: Credit-Information
      type: object
      properties:
        firstname:
          type: string
        last name:
          type: string
        sumCredit:
          type: number
          format: double
        interestRate:
          type: number
          format: double
        loanPeriod:
          type: integer
        dateCredit:
          type: string
          format: date
  securitySchemes: {}
